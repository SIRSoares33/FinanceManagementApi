<button class="logout-btn" @onclick="@OnClick.Invoke">
    <i class="fas fa-sign-out-alt"></i>
    @Text
</button>

@code {
    [Parameter] public required Func<Task> OnClick { get; set; }
    [Parameter] public required string Text { get; set; }
}
